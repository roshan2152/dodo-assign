apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: prod
data:
  REDIS_HOST: redis
  DB_HOST: db
  DB_PORT: "5432"
  DB_NAME: postgres
  DB_USER: postgres
  DB_PASSWORD: postgres
